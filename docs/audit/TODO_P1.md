# TODO P1 (Важные, но не блокирующие)

- [ ] Добавить автоматическую проверку PEP 8 и статический анализ
Почему это важно:
Сейчас стиль кода формально не подтверждается автоматикой; это риск замечаний на защите.
Что изменить (конкретные файлы):
`requirements.txt` (ruff/flake8), `pyproject.toml` или `.ruff.toml`/`.flake8`, README (раздел про lint).
Критерий приемки (Definition of Done):
`ruff check app tests` (или `flake8 app tests`) проходит без ошибок.

- [ ] Унифицировать DB-layer (избежать дублирования `app/core/db.py` и `app/db/session.py`)
Почему это важно:
Два похожих модуля подключения к БД ухудшают поддерживаемость и повышают риск рассинхронизации.
Что изменить (конкретные файлы):
`app/core/db.py`, `app/db/session.py`, импорты в местах использования.
Критерий приемки (Definition of Done):
В проекте остается один канонический модуль подключения к БД, все зависимости идут через него.

- [ ] Расширить `/health/ready` проверкой PostgreSQL
Почему это важно:
Сейчас readiness учитывает только Redis, но не подтверждает готовность БД.
Что изменить (конкретные файлы):
`app/routers/health.py`, `app/core/db.py`, `app/schemas/health.py`, тесты health.
Критерий приемки (Definition of Done):
`/health/ready` возвращает статус по Redis и PostgreSQL, тесты учитывают оба компонента.

- [ ] Добавить использование `audit_log` в чувствительных операциях
Почему это важно:
Таблица `audit_log` есть в модели, но практическая трассировка действий сейчас не показана.
Что изменить (конкретные файлы):
`app/services/purchase_service.py`, `app/services/refund_service.py`, `app/services/admin_item_service.py`, репозитории/ORM слой после DB-перехода.
Критерий приемки (Definition of Done):
После ключевых операций в БД появляются аудиторские записи с actor/action/entity.
