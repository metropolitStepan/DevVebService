# TODO P2 (Улучшения и рефакторинг)

- [ ] Добавить краткие комментарии в наиболее сложные транзакционные участки
Почему это полезно:
Ускоряет ревью и поддержку, снижает риск неправильных изменений в будущем.
Что изменить (конкретные файлы):
`app/db/repositories/purchases.py`, `app/db/repositories/refunds.py`, `app/services/purchase_service.py`.
Критерий приемки (Definition of Done):
Есть краткие, предметные комментарии к шагам транзакции/идемпотентности без дублирования очевидного.

- [ ] Уточнить документацию по 3НФ и ограничениям доменной модели
Почему это полезно:
Упрощает защиту проекта и объяснение архитектурных решений.
Что изменить (конкретные файлы):
`docs/` (отдельный md с обоснованием 3НФ), `README.md` (ссылка).
Критерий приемки (Definition of Done):
Есть короткий документ с обоснованием 3НФ по ключевым таблицам и trade-offs MVP.

- [ ] Расширить примеры API в README для ошибок (400/401/403/409/422)
Почему это полезно:
Повышает эксплуатационную понятность API и сокращает вопросы при проверке.
Что изменить (конкретные файлы):
`README.md`.
Критерий приемки (Definition of Done):
Для 2–3 ключевых endpoint добавлены примеры негативных ответов в едином формате ошибок.

- [ ] Добавить make/just команды для стандартных операций
Почему это полезно:
Стандартизирует запуск `up/migrate/test/lint/down` и ускоряет проверку.
Что изменить (конкретные файлы):
`Makefile` или `justfile`, `README.md`.
Критерий приемки (Definition of Done):
Команды запускаются одной строкой и соответствуют инструкциям README.
